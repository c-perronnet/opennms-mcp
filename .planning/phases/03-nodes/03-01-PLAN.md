---
phase: 03-nodes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/nodes.ts
  - src/index.ts
autonomous: true
requirements: [NODE-01, NODE-02, NODE-03, NODE-04]

must_haves:
  truths:
    - "Claude can list all nodes and see node ID, label, foreignSource, foreignId, and location"
    - "Claude can filter node list by label (e.g. 'label==web*') or by category (e.g. 'category.name==Routers')"
    - "Claude can retrieve a specific node by numeric ID and see full details"
    - "Claude can retrieve a specific node by foreignSource:foreignId format (e.g. 'MySource:server-001')"
    - "Claude can list all IP interfaces for a node and see IP address, hostname, managed status, and SNMP primary flag"
    - "Claude can list all SNMP interfaces for a node and see ifIndex, ifDescr, ifName, admin/oper status"
    - "Empty results return a user-friendly message, not an error"
  artifacts:
    - path: "src/tools/nodes.ts"
      provides: "NodeDTO, IpInterfaceDTO, SnmpInterfaceDTO interfaces; formatters; registerNodeTools() exporting list_nodes, get_node, get_node_ip_interfaces, get_node_snmp_interfaces"
      exports: ["registerNodeTools", "NodeDTO"]
    - path: "src/index.ts"
      provides: "registerNodeTools wired in after registerAlarmTools, before server_info"
      contains: "registerNodeTools(server, client, config)"
  key_links:
    - from: "src/index.ts"
      to: "src/tools/nodes.ts"
      via: "import { registerNodeTools } from './tools/nodes.js'"
      pattern: "registerNodeTools\\(server, client, config\\)"
    - from: "src/tools/nodes.ts list_nodes"
      to: "/api/v2/nodes"
      via: "client.v2.get('/nodes', { params })"
      pattern: "client\\.v2\\.get.*nodes"
    - from: "src/tools/nodes.ts get_node_ip_interfaces"
      to: "/api/v2/nodes/{id}/ipinterfaces"
      via: "client.v2.get(`/nodes/${id}/ipinterfaces`)"
      pattern: "client\\.v2\\.get.*ipinterfaces"
---

<objective>
Create src/tools/nodes.ts with TypeScript interfaces, formatters, and four read MCP tools (list_nodes, get_node, get_node_ip_interfaces, get_node_snmp_interfaces), then wire registerNodeTools() into src/index.ts.

Purpose: Give Claude the ability to discover and inspect OpenNMS nodes and their interfaces — the foundation all node-centric workflows depend on.
Output: src/tools/nodes.ts (new), src/index.ts (modified — one import + one function call added)
</objective>

<execution_context>
@/home/c_perronnet/.claude/get-shit-done/workflows/execute-plan.md
@/home/c_perronnet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-nodes/03-RESEARCH.md
@.planning/phases/02-alarms/02-01-SUMMARY.md

<interfaces>
<!-- Established patterns to replicate exactly. No codebase exploration needed. -->

From src/client.ts:
```typescript
export function createApiClient(config: OpenNMSConfig) { ... }
export type ApiClient = ReturnType<typeof createApiClient>;
// client.v2: AxiosInstance with baseURL = `${config.url}/api/v2`
// client.v1: AxiosInstance with baseURL = `${config.url}/opennms/rest`
export function buildErrorMessage(err: unknown, context: string): string;
```

From src/config.ts:
```typescript
export type OpenNMSConfig = { url: string; insecure?: boolean; } & (
  { username: string; password: string } | { token: string }
);
```

From src/tools/alarms.ts (exact pattern to replicate):
```typescript
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { z } from "zod";
import { ApiClient, buildErrorMessage } from "../client.js";
import { OpenNMSConfig } from "../config.js";

export function registerAlarmTools(
  server: McpServer,
  client: ApiClient,
  _config: OpenNMSConfig,  // _config prefix: unused but kept for consistent 3-arg signature
): void {
  server.tool("tool_name", "description", { schema }, async (args) => {
    try {
      const params: Record<string, string | number> = { limit };
      if (filter) params._s = filter;  // axios encodes _s automatically — no encodeURIComponent
      const resp = await client.v2.get("/alarms", { params });

      // 204 guard — v2 returns 204 No Content on empty results, not 200 with empty array
      if (resp.status === 204 || !resp.data?.alarm?.length) {
        return { content: [{ type: "text", text: "No alarms found matching the given filter." }] };
      }
      // ...
    } catch (err) {
      return { content: [{ type: "text", text: buildErrorMessage(err, "list alarms") }], isError: true };
    }
  });
}
```

From src/index.ts (current state — add registerNodeTools after existing registerAlarmTools call):
```typescript
import { registerAlarmTools } from "./tools/alarms.js";
// ...
// Step 5: Register alarm tools (Phase 2)
registerAlarmTools(server, client, config);
// Step 6: Register stub tool — server_info
server.tool("server_info", ...);
// Step 7: Connect transport
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/tools/nodes.ts with interfaces, formatters, and four read tools</name>
  <files>src/tools/nodes.ts</files>
  <action>
Create src/tools/nodes.ts from scratch. Write it verbatim as shown below — do not paraphrase or restructure.

```typescript
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { z } from "zod";
import { ApiClient, buildErrorMessage } from "../client.js";
import { OpenNMSConfig } from "../config.js";

// NodeDTO derived from OnmsNode.java JAXB annotations and UI types/index.ts
// CRITICAL: node.id is serialized as string (@XmlID), not number
interface NodeDTO {
  id: string;               // @XmlID — string even though internally Integer
  label: string;
  type?: string;            // "A"=active, "D"=deleted
  foreignSource?: string;
  foreignId?: string;
  location?: string;
  sysName?: string;
  sysDescription?: string;
  sysObjectId?: string;
  sysLocation?: string;
  sysContact?: string;
  createTime?: number;      // epoch ms
  lastCapsdPoll?: string;
}

// IpInterfaceDTO derived from OnmsIpInterface.java and UI types/index.ts
interface IpInterfaceDTO {
  id: string;               // @XmlID — string
  ipAddress: string;
  hostName?: string;
  isManaged?: string;       // "M"=managed, "U"=unmanaged
  snmpPrimary?: string;     // "P"=primary, "S"=secondary, "N"=not eligible
  ifIndex?: number;
  isDown?: boolean;
  nodeId?: number;
  lastCapsdPoll?: number;
}

// SnmpInterfaceDTO derived from OnmsSnmpInterface.java and UI types/index.ts
// NOTE: ifDescr, ifName, etc. lack @XmlAttribute annotations in Java source;
// presence in JSON is inferred from UI TypeScript types — all fields marked optional.
interface SnmpInterfaceDTO {
  id: number;
  ifIndex?: number;
  ifDescr?: string;
  ifName?: string;
  ifAlias?: string;
  ifSpeed?: number;
  ifAdminStatus?: number;   // 1=up, 2=down, 3=testing
  ifOperStatus?: number;    // 1=up, 2=down
  ifType?: number;
  physAddr?: string;
  collectFlag?: string;
  collect?: boolean;
  poll?: boolean;
}

// Format a node as a one-entry summary block (used in list_nodes)
function formatNodeSummary(node: NodeDTO): string {
  const fs = node.foreignSource && node.foreignId
    ? `  ForeignSource: ${node.foreignSource}  ForeignId: ${node.foreignId}`
    : "";
  const loc = node.location ? `  Location: ${node.location}` : "";
  return [
    `ID: ${node.id}  Label: ${node.label}${loc}`,
    fs,
  ].filter(Boolean).join("\n");
}

// Format full node detail (used in get_node)
function formatNodeDetail(node: NodeDTO): string {
  const lines: string[] = [
    `Node ID: ${node.id}`,
    `Label: ${node.label}`,
    `Type: ${node.type ?? "unknown"}`,
    `Foreign Source: ${node.foreignSource ?? "none"}`,
    `Foreign ID: ${node.foreignId ?? "none"}`,
    `Location: ${node.location ?? "none"}`,
    `SysName: ${node.sysName ?? "none"}`,
    `SysDescription: ${node.sysDescription ?? "none"}`,
    `SysObjectId: ${node.sysObjectId ?? "none"}`,
    `SysLocation: ${node.sysLocation ?? "none"}`,
    `SysContact: ${node.sysContact ?? "none"}`,
    `Created: ${node.createTime ? new Date(node.createTime).toISOString() : "unknown"}`,
    `Last Poll: ${node.lastCapsdPoll ?? "unknown"}`,
  ];
  return lines.join("\n");
}

// Format a single IP interface as a summary line
function formatIpInterface(iface: IpInterfaceDTO): string {
  const managed = iface.isManaged === "M" ? "managed" : iface.isManaged === "U" ? "unmanaged" : iface.isManaged ?? "unknown";
  const primary = iface.snmpPrimary === "P" ? "SNMP primary" : iface.snmpPrimary === "S" ? "SNMP secondary" : "";
  const down = iface.isDown ? "  [DOWN]" : "";
  const parts = [
    `IP: ${iface.ipAddress}`,
    iface.hostName ? `Host: ${iface.hostName}` : null,
    `Managed: ${managed}`,
    primary || null,
    iface.ifIndex ? `ifIndex: ${iface.ifIndex}` : null,
  ].filter(Boolean);
  return parts.join("  ") + down;
}

// Format a single SNMP interface as a summary line
function formatSnmpInterface(iface: SnmpInterfaceDTO): string {
  const adminStatus = iface.ifAdminStatus === 1 ? "admin:up" : iface.ifAdminStatus === 2 ? "admin:down" : `admin:${iface.ifAdminStatus ?? "?"}`;
  const operStatus = iface.ifOperStatus === 1 ? "oper:up" : iface.ifOperStatus === 2 ? "oper:down" : `oper:${iface.ifOperStatus ?? "?"}`;
  const parts = [
    iface.ifIndex != null ? `ifIndex: ${iface.ifIndex}` : null,
    iface.ifName ? `Name: ${iface.ifName}` : null,
    iface.ifDescr ? `Descr: ${iface.ifDescr}` : null,
    iface.ifAlias ? `Alias: ${iface.ifAlias}` : null,
    adminStatus,
    operStatus,
    iface.physAddr ? `MAC: ${iface.physAddr}` : null,
  ].filter(Boolean);
  return parts.join("  ");
}

export function registerNodeTools(
  server: McpServer,
  client: ApiClient,
  _config: OpenNMSConfig,
): void {

  // NODE-01: List nodes with optional FIQL filter by label or category
  server.tool(
    "list_nodes",
    "List OpenNMS nodes. Optionally filter by label or category using FIQL. Examples: 'label==myserver', 'label==web*' (wildcard), 'category.name==Routers', 'foreignSource==MySource'. IMPORTANT: use 'category.name==X' not 'category==X'. Returns node ID, label, foreignSource, foreignId, and location for each node.",
    {
      filter: z.string().optional().describe(
        "FIQL filter. Examples: 'label==myserver', 'label==web*' (wildcard), 'category.name==Routers', 'foreignSource==MyImportSource'. Omit for all nodes. Do NOT URL-encode."
      ),
      limit: z.number().int().min(1).max(1000).default(25).describe(
        "Maximum number of nodes to return (default 25, max 1000)."
      ),
    },
    async ({ filter, limit }) => {
      try {
        const params: Record<string, string | number> = { limit };
        if (filter) params._s = filter;
        // Do NOT call encodeURIComponent — axios encodes automatically; pre-encoding doubles it.
        const resp = await client.v2.get("/nodes", { params });

        // v2 returns HTTP 204 No Content (not 200 + empty array) when no nodes match.
        if (resp.status === 204 || !resp.data?.node?.length) {
          return { content: [{ type: "text", text: "No nodes found matching the given filter." }] };
        }

        // CRITICAL: array key is "node" (singular), not "nodes" — @JsonProperty("node") in OnmsNodeList.java
        const nodes = resp.data.node as NodeDTO[];
        const totalCount: number = resp.data.totalCount ?? nodes.length;
        const lines = nodes.map(formatNodeSummary);
        const header = `Nodes: ${nodes.length} of ${totalCount} total`;
        return { content: [{ type: "text", text: [header, "", ...lines].join("\n") }] };
      } catch (err) {
        return { content: [{ type: "text", text: buildErrorMessage(err, "list nodes") }], isError: true };
      }
    }
  );

  // NODE-02: Get a specific node by numeric ID or foreignSource:foreignId
  server.tool(
    "get_node",
    "Get full details for a specific OpenNMS node. Accepts either a numeric node ID (e.g. '42') or 'foreignSource:foreignId' format (e.g. 'MySource:server-001'). The v2 API resolves both formats transparently — no separate lookup needed.",
    {
      id: z.string().describe(
        "Node identifier: numeric ID (e.g. '42') or foreignSource:foreignId format (e.g. 'MySource:server-001')."
      ),
    },
    async ({ id }) => {
      try {
        const resp = await client.v2.get(`/nodes/${id}`);
        const node = resp.data as NodeDTO;
        return { content: [{ type: "text", text: formatNodeDetail(node) }] };
      } catch (err) {
        return { content: [{ type: "text", text: buildErrorMessage(err, `node ${id}`) }], isError: true };
      }
    }
  );

  // NODE-03: List IP interfaces for a node
  server.tool(
    "get_node_ip_interfaces",
    "List IP interfaces for an OpenNMS node. Accepts numeric node ID or 'foreignSource:foreignId' format. Returns IP address, hostname, managed status, SNMP primary flag, and ifIndex for each interface.",
    {
      id: z.string().describe(
        "Node identifier: numeric ID (e.g. '42') or foreignSource:foreignId format (e.g. 'MySource:server-001')."
      ),
    },
    async ({ id }) => {
      try {
        const resp = await client.v2.get(`/nodes/${id}/ipinterfaces`);

        // CRITICAL: array key is "ipInterface" (singular), not "ipInterfaces"
        if (resp.status === 204 || !resp.data?.ipInterface?.length) {
          return { content: [{ type: "text", text: `No IP interfaces found for node ${id}.` }] };
        }

        const interfaces = resp.data.ipInterface as IpInterfaceDTO[];
        const totalCount: number = resp.data.totalCount ?? interfaces.length;
        const lines = interfaces.map(formatIpInterface);
        const header = `IP Interfaces for node ${id}: ${interfaces.length} of ${totalCount} total`;
        return { content: [{ type: "text", text: [header, "", ...lines].join("\n") }] };
      } catch (err) {
        return { content: [{ type: "text", text: buildErrorMessage(err, `IP interfaces for node ${id}`) }], isError: true };
      }
    }
  );

  // NODE-04: List SNMP interfaces for a node
  server.tool(
    "get_node_snmp_interfaces",
    "List SNMP interfaces for an OpenNMS node. Accepts numeric node ID or 'foreignSource:foreignId' format. Returns ifIndex, ifName, ifDescr, ifAlias, admin/oper status, and MAC address for each interface.",
    {
      id: z.string().describe(
        "Node identifier: numeric ID (e.g. '42') or foreignSource:foreignId format (e.g. 'MySource:server-001')."
      ),
    },
    async ({ id }) => {
      try {
        const resp = await client.v2.get(`/nodes/${id}/snmpinterfaces`);

        // CRITICAL: array key is "snmpInterface" (singular), not "snmpInterfaces"
        if (resp.status === 204 || !resp.data?.snmpInterface?.length) {
          return { content: [{ type: "text", text: `No SNMP interfaces found for node ${id}.` }] };
        }

        const interfaces = resp.data.snmpInterface as SnmpInterfaceDTO[];
        const totalCount: number = resp.data.totalCount ?? interfaces.length;
        const lines = interfaces.map(formatSnmpInterface);
        const header = `SNMP Interfaces for node ${id}: ${interfaces.length} of ${totalCount} total`;
        return { content: [{ type: "text", text: [header, "", ...lines].join("\n") }] };
      } catch (err) {
        return { content: [{ type: "text", text: buildErrorMessage(err, `SNMP interfaces for node ${id}`) }], isError: true };
      }
    }
  );

}
```
  </action>
  <verify>
    <automated>cd /home/c_perronnet/git/opennms-mcp && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>src/tools/nodes.ts exists and compiles without TypeScript errors. File exports registerNodeTools() with list_nodes, get_node, get_node_ip_interfaces, get_node_snmp_interfaces tools.</done>
</task>

<task type="auto">
  <name>Task 2: Wire registerNodeTools into src/index.ts</name>
  <files>src/index.ts</files>
  <action>
Add two lines to src/index.ts — one import and one function call. Follow the established pattern from registerAlarmTools exactly.

Current src/index.ts has:
```typescript
import { registerAlarmTools } from "./tools/alarms.js";
// ...
// Step 5: Register alarm tools (Phase 2)
registerAlarmTools(server, client, config);

// Step 6: Register stub tool — server_info
```

Make the following minimal changes:

1. Add import after the existing registerAlarmTools import line:
```typescript
import { registerNodeTools } from "./tools/nodes.js";
```

2. Add a registration call between the registerAlarmTools call (Step 5) and the server_info tool (Step 6):
```typescript
// Step 6: Register node tools (Phase 3)
registerNodeTools(server, client, config);
```

3. Renumber the subsequent steps: server_info becomes Step 7, transport becomes Step 8.

The final import section should look like:
```typescript
import { registerAlarmTools } from "./tools/alarms.js";
import { registerNodeTools } from "./tools/nodes.js";
```

The registration section should look like:
```typescript
// Step 5: Register alarm tools (Phase 2)
registerAlarmTools(server, client, config);

// Step 6: Register node tools (Phase 3)
registerNodeTools(server, client, config);

// Step 7: Register stub tool — server_info
server.tool("server_info", ...);

// Step 8: Connect transport (FOUND-04) — must come AFTER all registerTool calls
```

Do not change any other lines in src/index.ts.
  </action>
  <verify>
    <automated>cd /home/c_perronnet/git/opennms-mcp && npx tsc --noEmit 2>&1 | head -30 && grep -n "registerNodeTools" src/index.ts</automated>
  </verify>
  <done>src/index.ts compiles without errors. grep shows registerNodeTools import and call both present. `npx tsc --noEmit` exits 0.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full build:

```bash
cd /home/c_perronnet/git/opennms-mcp && npx tsc --noEmit
```

Must exit 0 with no errors.

Additionally confirm all four tools are registered:
```bash
grep -n "server.tool" /home/c_perronnet/git/opennms-mcp/src/tools/nodes.ts
```

Must show: list_nodes, get_node, get_node_ip_interfaces, get_node_snmp_interfaces (4 tool registrations).
</verification>

<success_criteria>
- src/tools/nodes.ts created with NodeDTO, IpInterfaceDTO, SnmpInterfaceDTO interfaces, all formatter functions, and registerNodeTools() exporting 4 tools
- src/index.ts updated with import + call for registerNodeTools — no other changes
- `npx tsc --noEmit` exits 0 (no TypeScript errors)
- All 4 tools use correct singular array keys: "node", "ipInterface", "snmpInterface"
- All 3 list tools have 204/empty guard before accessing array
- NODE-01, NODE-02, NODE-03, NODE-04 implemented
</success_criteria>

<output>
After completion, create `.planning/phases/03-nodes/03-01-SUMMARY.md` using the summary template at @/home/c_perronnet/.claude/get-shit-done/templates/summary.md
</output>
